# DZ-12 — AirGuard C1: HIL-стенд та E2E-автоматизація

## Обраний IoT-продукт

**AirGuard C1** — розумний настільний монітор якості повітря для дому/офісу.

Пристрій вимірює концентрацію CO2 (еквівалент), температуру та відносну вологість, показує
стан повітря світлодіодом і звуковим сигналом, а також публікує показники у JSON по MQTT —
щоб значення бачив мобільний застосунок / дашборд користувача.

| Параметр | Значення |
|---|---|
| DUT | ESP32-S3 DevKitC-1 (N16R8) |
| Сенсор CO2 | MQ-135 (аналоговий, 0–3.3 В, 400–5000 ppm) |
| Сенсор температури/вологості | SHT31 (I2C) |
| Індикація | RGB LED (стан повітря), buzzer (тривога) |
| Мережа | WiFi 2.4 ГГц → MQTT-брокер |
| Інтерфейс QA | CLI через UART (USB) 115200 8N1 |
| Живлення | 5 В USB (керується реле HIL-стенда) |

## Чому саме цей продукт

Він покриває повний стек курсу:
- **6-ланковий E2E-ланцюжок** з Lesson 12: фізичний вплив → сенсор → DUT → WiFi → MQTT-брокер → підписник;
- **signal injection** (Lesson 10): DAC подає відому напругу на аналоговий вхід MQ-135 замість сенсора;
- **race/leak/stability тести** (Lesson 12, hard mode): конфіг у NVS, MQTT-клієнт у прошивці — класичні місця для race та memory leak;
- **простий у Wokwi**: аналоговий сенсор = потенціометр, тривога = LED — ключовий сценарій «CO2 → тривога → MQTT» моделюється в браузері.

## Склад документації

| Файл | Зміст |
|---|---|
| `PRD.md` | Вимоги до продукту (функціональні FR, нефункціональні NFR, критичні сценарії) |
| `stand_diagram.png` | Графічна схема HIL-стенда |
| `project_structure.md` | Дерево тестового проєкту (без коду тестів) |
| `traceability_matrix.md` | Матриця трасованості «Вимога → Тест-файл → Тест-функція» (повне покриття) |
| `wokwi_link.md` | Wokwi-модель ключового тестового сценарію + інструкція |
| `wokwi/diagram.json`, `wokwi/sketch.ino` | Файли Wokwi-моделі (готові до вставки у wokwi.com) |

## Як читати комплект

1. `PRD.md` — джерело істини: 14 функціональних і 10 нефункціональних вимог з ID (FR-xxx / NFR-xxx).
2. `stand_diagram.png` — як фізично зібраний стенд перевіряє ці вимоги (Power Control, Signal Injection, Observability, MQTT-перехоплення).
3. `project_structure.md` — як організований pytest-проєкт (drivers / tests: smoke-functional-e2e-stability / fixtures).
4. `traceability_matrix.md` — доказ покриття: кожна вимога PRD → конкретний тест-файл і тест-функція.